# Suppose your top-level clock is "clk", which is 50 MHz => 20 ns period:
create_clock -name {clk} -period 20.000 [get_ports {clk}]

# If you have more than one clock, or a PLL, define them as well.
# For example, if you have an output of a PLL named 'clk_100mhz':
# create_generated_clock -name {clk_100mhz} -period 10.000 [get_pins {pll_inst|clk_out}]

# If your external inputs or outputs need constraints, e.g. for setup/hold:
# set_input_delay -clock clk 3.0 [get_ports {some_input}]
# set_output_delay -clock clk 2.0 [get_ports {some_output}]
